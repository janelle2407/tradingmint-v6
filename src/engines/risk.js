function applyCosts(price,side,settings){const slippage=Number(settings.slippagePct||0)/100,spread=Number(settings.spreadPct||0)/100;if(side==="buy")return price*(1+slippage+spread/2);if(side==="sell")return price*(1-slippage-spread/2);return price;}
function accountEquity(account){return account.cash+account.open.reduce((s,p)=>s+Number(p.lastPrice||p.entry)*Number(p.shares),0);}
function dailyPnl(account){const today=new Date().toISOString().slice(0,10);return account.closed.filter(p=>String(p.exitTime||"").startsWith(today)).reduce((s,p)=>s+Number(p.pnl||0),0);}
function riskLockout(account,settings,stats={}){const starting=Number(settings.startingCash||5000),dailyLoss=dailyPnl(account),dailyLossPct=starting?Math.abs(Math.min(0,dailyLoss))/starting*100:0;if(dailyLossPct>=Number(settings.maxDailyLossPct||3))return{locked:true,reason:`Daily loss lockout hit: ${dailyLossPct.toFixed(2)}%.`};if(Number(stats.maxDrawdown||0)>=Number(settings.maxDrawdownPct||10))return{locked:true,reason:`Max drawdown lockout hit: ${stats.maxDrawdown}%.`};return{locked:false,reason:""};}
function positionSize(account,signal,settings){const equity=accountEquity(account),maxTradeValue=equity*(Number(settings.maxTradePct||20)/100),entryCost=applyCosts(signal.entry,"buy",settings),shares=Math.floor(maxTradeValue/entryCost);return Math.max(0,shares);}
function canEnter(account,signal,settings,stats={}){const reasons=[],lockout=riskLockout(account,settings,stats);if(lockout.locked)reasons.push(lockout.reason);if(!signal)reasons.push("No signal.");if(signal&&signal.safety!=="TRADE_READY")reasons.push("Signal is not TRADE_READY.");if(signal&&Number(signal.confidence)<Number(settings.minConfidence))reasons.push("Confidence below minimum.");if(signal&&Number(signal.rrNumber)<Number(settings.minRiskReward))reasons.push("Risk/reward below minimum.");if(signal&&settings.requireHistoricalEdge&&(!signal.historicalStats||Number(signal.historicalStats.trades||0)<Number(settings.minHistoricalTrades)))reasons.push("Historical sample too small.");if(account.open.length>=Number(settings.maxOpenPositions))reasons.push("Max open positions reached.");if(signal&&account.open.some(pos=>pos.symbol===signal.symbol))reasons.push("Position already open for symbol.");const today=new Date().toISOString().slice(0,10),entriesToday=account.open.filter(p=>String(p.entryTime||"").startsWith(today)).length+account.closed.filter(p=>String(p.entryTime||"").startsWith(today)).length;if(entriesToday>=Number(settings.maxDailyEntries))reasons.push("Max daily entries reached.");const shares=signal?positionSize(account,signal,settings):0;if(shares<=0)reasons.push("Not enough cash for position size.");return{ok:reasons.length===0,reasons,shares,lockout};}
module.exports={applyCosts,positionSize,canEnter,accountEquity,dailyPnl,riskLockout};