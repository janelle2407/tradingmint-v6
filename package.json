{
  "name": "tradingmint-pro",
  "version": "7.4.0",
  "description": "TradingMint PRO v7.4 \u2014 US stock swing-trader scanner with scanner-equivalent backtesting, intraday RVOL, safer earnings defaults, portfolio constraints, and paper-only execution.",
  "main": "src/server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node src/server.js",
    "test": "node tests/runTests.js",
    "seed": "node scripts/seedHistoricalData.js",
    "check": "node --check src/server.js && node --check src/engines/scanner.js && node --check src/engines/backtest.js && node --check src/engines/paper.js && node --check src/data/marketData.js && node tests/runTests.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.19.2"
  },
  "optionalDependencies": {
    "better-sqlite3": "^11.8.1"
  }
}
